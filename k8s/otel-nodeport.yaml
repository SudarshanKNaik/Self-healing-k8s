apiVersion: v1
kind: Service
metadata:
  name: otel-collector-external
  namespace: default
  labels:
    app.kubernetes.io/name: opentelemetry-collector
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: opentelemetry-collector
  ports:
    - name: otlp-http
      port: 4318
      targetPort: 4318
      nodePort: 30080
